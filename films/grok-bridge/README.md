# Grok Bridge film

Remotion walkthrough of the real-world path in [docs/grok-bridge.md](../../docs/grok-bridge.md).

Monday, `#pipeline-review`. Maya asks Sara. Sara runs on Divyansh’s Grok Bot (where the CRM is signed in) and posts the watch list back into the QM room.

```bash
cd films/grok-bridge
npm install
npm run dev      # Remotion Studio
npm run render   # writes out/grok-bridge.mp4
```

A tracked preview (54s, 1920×1080) is [grok-bridge.mp4](./grok-bridge.mp4). `npm run render` also writes `out/grok-bridge.mp4`.
